{{>header}}

{{#if imports}}

{{#each imports}}
import type { {{{this}}} } from './{{{this}}}';
{{/each}}
{{/if}}

{{#equals export 'interface'}}
// interface
{{>exportInterface}}
{{else equals export 'enum'}}
// enum
{{>exportEnum}}
{{else}}
// type
{{>exportType}}
{{/equals}}
